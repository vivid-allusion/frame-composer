[project]
name = "frame-composer"
version = "2.1.0"
description = "Vivid Allusion Frame Composer"
requires-python = ">=3.10"
dependencies = [
    "loguru",
    "pyyaml",
    "python-dotenv",
]

[project.scripts]
frame-composer = "src.main_simple:main"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
