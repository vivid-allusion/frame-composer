# Replicate

# Model endpoint (required)
endpoint: "google/nano-banana-2"

# Description for reports (optional)
description: "Gemini 3 - Nano Banana 2"

# Pricing information (optional, for cost tracking)
pricing:
  base_cost: 0.014
  currency: "USD"

# Model parameters (required) - all parameters passed through to API
parameters:
  num_images: 1
  aspect_ratio: "21:9"
  output_format: "png"
  resolution: "1K"
  safety_filter_level: "block_only_high"

# Fixes
prompt_prefix: 'A cinematic scene from a historical feature film in color.'
prompt_suffix: 'As if shot on 35mm film by Sir Roger Deakins with diagonal composition.'
